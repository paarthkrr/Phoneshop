{
  "name": "shop-backend",
  "version": "1.0.0",
  "description": "Real storage backend for the phone shop tools — replaces Claude's window.storage with HTTP + SQLite.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
